# Ignore large media / test clips (keep repo light)
clips/*.mp4
clips/*.mov
clips/*.webm
clips/*.mkv
clips/*.jpg
*.mp4
*.mov
.DS_Store
__pycache__/
*.pyc

# Dev/test deps (jsdom, canvas) — nao sao do simulador
node_modules/
package-lock.json
package.json

# Backend bridge (venv + queue runtime)
.venv/
queue.json

# Secrets (numero WhatsApp) — nunca comitar
.env
# Base de dados financeira local (resultados reais)
finance.db
# Ficheiros de plano de negocio (gerados localmente, pesados em binarios)
negocios/
clips_shorts_hud/
assets/hud_vertical.png
videos/clips/
videos/shorts/
videos/completos/
clips_shorts_hud/
clips/
